-- ============================================================
-- Seed Users Template
-- Run AFTER each person has signed in with Google at least once.
-- Replace the placeholder values with real member information.
-- ============================================================

-- Example: set biblical handle for a member who already signed in
-- UPDATE public.users
-- SET biblical_handle = 'Ruth', onboarding_complete = true, bio = 'Excited to read with the family!'
-- WHERE email = 'ruth.example@gmail.com';

-- Example: promote to admin
-- UPDATE public.users SET role = 'admin' WHERE email = 'steward@gmail.com';

-- Example: make super admin (do this for yourself first)
-- UPDATE public.users SET role = 'super_admin' WHERE email = 'you@gmail.com';

-- ============================================================
-- SEND THE DEVELOPER / STEWARD THIS INFO FOR EACH MEMBER:
--   1. Full name (admin only — from Google)
--   2. Gmail address (must match Google sign-in)
--   3. Desired biblical handle (e.g. Boaz, Esther, Timothy)
-- ============================================================
